apiVersion: v1
kind: Pod
metadata:
  name: hello-world
  namespace: test
  labels:
    app: hello-world
spec:
  containers:
    - name: hello-world
      image: hello-world
      ports:
        - containerPort: 80
